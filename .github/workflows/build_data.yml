name: update RRP data
on:
  push:
    branches:
      - cicd

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: "Build & Run"
        run: |
          ls -l
          echo "hi..."
          pwd
          npm install
          node backend/index.js
          echo "hello.."
